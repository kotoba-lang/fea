;; schema.edn — Datomic/Datascript 互換スキーマ定義（自動生成 by scripts/edn-datomize.bb）
;; :db/ident 属性定義のリスト。Datomic 固有キー(:db.install/_attribute 等)は使わない。
;; 手編集禁止 — 再生成すると上書きされる。

[#:db{:ident :kami.fea.material/model, :valueType :db.type/string, :cardinality :db.cardinality/one} #:db{:ident :kami.fea.material/name, :valueType :db.type/string, :cardinality :db.cardinality/one}]
